      <p id="notice">
        <%= notice %>
      </p>

<div class="profile-page">
<div class="wrapper">

    <div class="page-header page-header-small" filter-color="green">
      <div class="page-header-image" data-parallax="true" style="background-image: url(<%= image_path 'pe.jpg' %>) ;">
      </div>
      <div class="content-center">
        <div class="photo-container">
          <%= image_tag @user.avatar, class:'rounded-circle img-raised', alt:"Photo de profile"%>
        </div>
        <h3 class="title"><%= @user.user_name %></h3>
        <p><%= @user.email %></p>
        <div class="content">
          <div class="social-description">
            <h2><%= @user.posts.size %></h2>
            <p>Articles
            </p>
          </div>
          <div class="social-description">
            <h2><%= @user.comments.size %></h2>
            <p>Commentaires</p>
          </div>
          <div class="social-description">
            <h2><%= @user.likes.size %></h2>
            <p>likes</p>
          </div>
        </div>
      </div>
    </div>

  <div class="section">
    <div class="container">
      <div class="button-container">
        <%= link_to edit_user_path(@user), class:"btn btn-primary btn-round btn-lg" do %>Editer mon profile<% end %>
        <%= link_to edit_user_registration_path, class:"btn btn-default btn-round btn-lg btn-icon", name:'Editer informations de connexion' do %>
        <i class="now-ui-icons ui-1_settings-gear-63" title="Editer infos de connexion"></i><% end %>
       </div>
      <div>
        <h3 class="title">À propos de moi
          <%= link_to edit_user_path(@user), class:"btn btn-primary btn-round btn-lg btn-icon", aria: {hidden:"true"} do %><i class="fa fa-pencil"></i>
          <% end %>
        </h3>
      </div>
      <h5 class="description text-center">An artist of considerable range, Ryan — the name taken by Melbourne-raised, Brooklyn-based Nick Murphy — writes, performs and records all of his own music, giving it a warm, intimate feel with a solid groove
      structure. An artist of considerable range.</h5>
      <div class="row"></div>
    </div>

    <div class="container">
      <div class="row">
        <div class="col-2"></div>
        <div class="col-4">
        <div class="card">
          <div class="card-header-center">
            <center>
              <h4 class="card-title-center">Catégories</h4>
            </center>
          </div>
          <ul class="list-group list-group-flush">
            <li class="list-group-item">
              <% @user.categories.each do |category| %>
              <span class="badge badge-primary"><%= category.name %></span>
              <% end %>
            </li>
          </ul>
        </div>
        </div>
        <div class="col-4">
        <div class="card">
          <div class="card-header-center">
            <center>
              <h4 class="card-title-center">Posts</h4>
            </center>
          </div>
          <ul class="list-group list-group-flush">
            <li class="list-group-item">
              <% @user.posts.limit(10).each do |post| %>
              <span class="badge badge-primary"><%= post.title %></span>
              <% end %>
            </li>
          </ul>
        </div>
        </div>
      </div>
    </div>

  </div>
</div>
</div>
    <br>
    <br>
    <br>